# style

Optional. Drop three to five articles here whose voice you want the brief to match,
and the agent reads them at the start of every run as models of sentence rhythm,
paragraph length, and attribution habits.

Section 8 of the system prompt already encodes WSJ house style in the abstract. This
folder is for calibrating against real examples. Use your own saved copies.

The agent uses these for voice only. It will not reproduce their sentences and will
not quote more than fifteen words from any one of them.
